apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: myapp
    tier: frontend
    environment: development
  annotations:
    description: "This is a pod with multiple labels and annotations"
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
      - containerPort: 80